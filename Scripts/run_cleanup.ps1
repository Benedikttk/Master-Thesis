# Navigate to the correct directory (if not already in the right directory)
cd "C:\Users\benja\Desktop\Speciale"

# Stage the changes (add deleted files to git)
git add -u

# Commit the changes with a message
git commit -m "Remove deleted files from repository"

# Push the changes to the remote repository
git push
